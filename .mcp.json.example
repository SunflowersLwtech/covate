{
  "mcpServers": {
    "covate": {
      "type": "stdio",
      "command": "${HOME}/covate/covate/bin/python",
      "args": ["-m", "covate"],
      "env": {
        "MCP_DEBUG": "${MCP_DEBUG:-false}"
      }
    }
  }
}
