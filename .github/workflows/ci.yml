name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Six build jobs across three operating systems: a superseded run verifies a
# commit nobody will ship. Safe to cancel — nothing here publishes anything.
concurrency:
  group: covate-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.11', '3.12']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e "."

      - name: Verify installation
        run: |
          python -c "import covate; print(f'Version: {covate.__version__}')"

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Ruff
        run: pip install ruff

      - name: Check code style
        run: ruff check src/ --ignore E501
