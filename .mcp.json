{
  "mcpServers": {
    "covate": {
      "type": "stdio",
      "command": "uvx",
      "args": ["--from", "${CLAUDE_PLUGIN_ROOT}", "covate"],
      "env": {
        "MCP_DEBUG": "false"
      }
    }
  }
}
